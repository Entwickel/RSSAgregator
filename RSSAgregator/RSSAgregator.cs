using System;
using System.Collections.Generic;
using System.Text;

namespace RSSAgregator
{
    class RSSagregator
    {
        public int x_fenetre;
        public int y_fenetre;


        public RSSagregator(int x, int y)
        {
            x_fenetre = x;
            y_fenetre = y;

        }

    }
}
